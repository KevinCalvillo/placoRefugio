import { VideoAdoptarPipe } from './video-adoptar.pipe';

describe('VideoAdoptarPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoAdoptarPipe();
    expect(pipe).toBeTruthy();
  });
});
